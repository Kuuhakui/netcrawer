# 🕸️ NetCrawler: Terminal Hacking Simulator

**NetCrawler** — это обучающий интерактивный симулятор на базе Node.js, погружающий пользователя в роль специалиста по кибербезопасности. Проект эмулирует работу в терминале Linux, взаимодействие с удаленными серверами, взлом сетевых протоколов и криптографический анализ.

> Проект разработан в качестве курсовой работы по дисциплине «Информационная безопасность и компьютерные сети».

## 🚀 Возможности

* **Эмуляция терминала:** Реалистичный интерфейс командной строки (CLI) с историей команд, автодополнением и цветным выводом.
* **Процедурная генерация:** Каждый уровень (подсеть) генерируется заново с уникальными IP-адресами, именами хостов и файловой системой.
* **Реальная криптография:** Механики взлома основаны на реальных алгоритмах:
    * 🔐 Шифр Цезаря (интерактивный подбор смещения).
    * 🔐 Шифр Виженера (частотный анализ и атака по словарю).
    * 🧬 Битовые операции XOR.
* **Сетевые атаки:**
    * Сканирование портов и хостов.
    * Брутфорс паролей.
    * Пассивный перехват пакетов (Sniffing) для кражи SSH-ключей.
    * Туннелирование (Pivot) через скомпрометированные роутеры.
* **Геймификация:** Система инвентаря, сбор фрагментов ключей и прогрессия сложности.

## 🛠️ Технологический стек

* **Язык:** TypeScript
* **Платформа:** Node.js
* **Библиотеки:**
    * `inquirer` — для интерактивных меню.
    * `chalk` — для стилизации вывода в консоль.
    * `uuid` — для генерации уникальных идентификаторов.

## 📦 Установка и Запуск

Для работы приложения требуется установленный **Node.js** (версии 14 или выше).

### 1. Клонирование репозитория

```bash
git clone [https://github.com/Kuuhakui/netcrawer.git](https://github.com/Kuuhakui/netcrawer.git)
```

```bash
cd netcrawler
```

### 2. Установка зависимостей

```Bash
npm install
```
### 3. Сборка проекта

Компиляция TypeScript файлов в JavaScript:
```Bash
npm run build
```

### 4. Запуск

```Bash
npm start
```

## Как играть (Руководство оператора)

Ваша цель — проникать в защищенные сети, собирать фрагменты данных и продвигаться вглубь системы.
Основные команды:
Команда	Описание	Пример
scan	Сканирование текущей сети. Показывает IP, имена и статус устройств.	scan
connect <IP>	Подключение к устройству. Запускает взлом, если доступа нет.	connect 192.168.0.15
ls	Список файлов в текущей директории (удаленной или локальной).	ls
cat <файл>	Чтение содержимого файла.	cat todo.txt
decrypt <файл>	Запуск дешифратора для защищенных файлов (.enc).	decrypt secure.enc
download <файл>	Скачивание файла на свой виртуальный ПК.	download key_frag.dat
sniff <IP>	(Ур. 2+) Запуск прослушки активной цели для перехвата SSH-ключа.	sniff 10.0.0.5
tunnel	(Router) Проброс туннеля на следующий уровень сложности.	tunnel
disconnect	Отключение от удаленного хоста.	disconnect
help	Вывод списка всех доступных команд.	help

Структура проекта

Архитектура приложения построена на принципах ООП с использованием паттернов Command и Singleton.
Plaintext

src/
├── app.ts                  # Точка входа в приложение
├── config/
│   └── settings.ts         # Типы и константы настроек
├── core/
│   ├── GameSession.ts      # Основной класс сессии (состояние игры)
│   ├── CommandProcessor.ts # Диспетчер команд (парсинг ввода)
│   ├── WorldGenerator.ts   # Алгоритмы процедурной генерации уровней
│   ├── SettingsManager.ts  # Менеджер настроек (Singleton)
│   ├── TutorialManager.ts  # Логика обучения
│   ├── Storage.ts          # Сохранение/загрузка (JSON)
│   └── commands/           # Реализация паттерна Command
│       ├── ICommand.ts     # Интерфейс команды
│       ├── FileCommands.ts # ls, cat, decrypt, download
│       └── NetworkCommands.ts # scan, connect, sniff, tunnel
├── interfaces/
│   ├── IDevice.ts          # Модель сетевого устройства
│   ├── IFileSystem.ts      # Модель файлов и типов шифрования
│   └── INetwork.ts         # Модель подсети
├── mechanics/
│   ├── CipherMechanics.ts  # Реализация криптоалгоритмов (Цезарь, Виженер, XOR)
│   └── HackingMinigame.ts  # Логика мини-игры с подбором паролей
└── ui/
    └── ConsoleUI.ts        # Обертка над readline/inquirer, анимации